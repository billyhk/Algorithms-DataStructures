function firstDuplicate(a: number[]): number {
    
    // create an object wherein we 
    // we're creating keys based off values 


    // failure case
    return -1;
}

console.log(firstDuplicate([2, 1, 3, 5, 3, 2]));
console.log(firstDuplicate([2, 4, 3, 5, 1]));
